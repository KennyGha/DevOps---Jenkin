pipeline {
    agent any
    stages {
       stage('Build') {
             steps {
                 
                 sh 'echo "Uploading content with AWS creds"'
                 
                 }
             }
        }
    }

